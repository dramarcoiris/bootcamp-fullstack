// Bucle de contador con for
for (i = 1; i <= 10; i++) {
    console.log(`Contador con for: ${i}`);
}

// Bucle de contador con while
let contador = 1;
while (contador <= 10) {
    console.log(`Contador con while: ${contador}`);
    contador++;
}
